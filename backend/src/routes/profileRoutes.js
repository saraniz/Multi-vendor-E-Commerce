// const express = require("express");
// const router = express.Router();
// const authenticate = require("../middleware/authMiddleware"); // Import your authenticate middleware
// const { getUserProfile } = require("../controller/profileController"); // Import controller

// // Route to fetch user profile (secured by authenticate middleware)
// router.get("/profile", authenticate, getUserProfile);

// module.exports = router;